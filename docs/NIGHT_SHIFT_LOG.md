# Night Shift Log

## 2026-07-11

- Read Phase 2 prompt and created branch `codex/phase-2-production-foundation`.
- Ran baseline commands and recorded results in `docs/PHASE_1_AUDIT.md`.
- Added GitHub Actions CI.
- Added PostgreSQL/Drizzle foundation and migrations.
- Implemented hashed session tokens and logout revocation.
- Added workflow, connector, and AI foundations.
- Fixed public publication to read immutable snapshots.
- Added public form idempotency, honeypot, and consent.
- Expanded tests from 3 to 6 tests.
- Confirmed PR #1 CI was green for the Phase 2 foundation checkpoint.
- Added password reset request/complete flows with hashed single-use tokens and session revocation.
- Added invitation creation, one-time acceptance, pending invitation display, and non-owner member role updates.
- Expanded tests from 6 to 10 tests.
- Added a PostgreSQL-only RLS integration test with a temporary restricted role.
- Confirmed CI green for commit `bb367bf`, including PostgreSQL RLS coverage.
- Started a small `next/image` cleanup for the remaining public-site `<img>` warning, but left it uncommitted because local Node-based validation commands hung repeatedly.
- Retried the `next/image` cleanup validation one hour later; `next/image` import and `git diff --check` passed, but ESLint import and `pnpm typecheck` still hung, so nothing was committed or pushed.
- Retried again; local Node validation still hung, but manual diff inspection and `git diff --check` passed, so the cleanup was prepared for CI validation on PR #1.
- Pushed the `next/image` cleanup as commit `093080f`; GitHub Actions passed and the previous `<img>` lint warning disappeared.
- Added durable batch processing for `domain_events`: processing claims, retry backoff, stale processing requeue, terminal failures, and targeted worker tests.
- Targeted local worker validation hung and `git diff --check` exited abnormally without output, so the worker checkpoint should be confirmed through GitHub Actions.
- Wired generic webhook HMAC verification to encrypted endpoint secrets with timestamp/signature headers and rejection delivery logs.
- Targeted local connector validation hung after runner startup, while `git diff --check` passed, so the HMAC checkpoint should be confirmed through GitHub Actions.
- Extracted authentication and session logic into `src/modules/auth/` with schemas, typed errors, repository functions, a domain service, and a direct module test.
- Targeted local auth validation hung without output and was stopped; `git diff --check` passed, so the auth extraction should be confirmed through GitHub Actions.
- Extracted tenant access, tenant creation, team invitations, member role management, and audit recording into `src/modules/tenants/` and `src/modules/audit/`, with a direct module test.
- Targeted local tenant validation hung and was stopped; `git diff --check` passed, so the tenant/team extraction should be confirmed through GitHub Actions.
- Extracted public website lead ingestion into `src/modules/crm/` with repository functions, schemas, typed errors, idempotent public form handling, audit preservation, and a direct idempotency test.
- Targeted local public lead validation hung without output and was stopped; continue to use `git diff --check` plus GitHub Actions for this checkpoint.
- Extracted website draft/publication responsibilities into `src/modules/websites/`, including workspace reads, draft edits, section reordering, publish, restore, immutable public snapshot reads, repository functions, schemas, typed errors, and a direct module test.
- Targeted local website validation and `git diff --check` both hung without output and were stopped; GitHub Actions passed for commit `033f062`.
- Extracted CRM read models, recent activity reads, and tenant-scoped contact lookup into `src/modules/crm/`, with a direct cross-tenant isolation test.
- Targeted local CRM validation hung without output and was stopped; `git diff --check` passed and GitHub Actions passed for commit `70d8ff9`.
- Extracted connector catalog, connector state reads, CSV imports, mock sync, webhook receipt, import rows, and webhook delivery logging into `src/modules/connectors/`, with a direct module test.
- Targeted local connectors validation hung without output and was stopped; `git diff --check` passed and GitHub Actions passed for commit `a9a55cf`.
- Added CRM contact detail and mutation depth in `src/modules/crm/`: profile edits, assignment validation, consent updates, notes, contact tasks, task completion, contact-linked opportunity reads, timeline activity, server actions, contact detail UI, and a tenant-isolation/audit integration test.
- Targeted local CRM mutation validation hung without output and was stopped; `git diff --check` passed. GitHub Actions first caught a lint issue on commit `ef4cb8c`, the page lint fix was pushed as `5877e3f`, and both PR #1 CI checks passed.
- Added CRM opportunity depth: listing with search/filter support, opportunity detail page, stage/value/next-action/lost-reason updates, tenant-scoped pipeline stage validation, audit events, contact timeline propagation, and an integration test.
- Targeted local CRM opportunity validation hung without output and was stopped; unstaged `git diff --check` exited abnormally, staged `git diff --cached --check` passed, and GitHub Actions passed for commit `0c892b2`.
- Added CRM duplicate handling: normalized duplicate candidate detection, duplicate review and comparison UI, user-selected survivor, field-level merge choices, explicit confirmation, merge reason, transaction-backed reassignment of related records, merge tombstones, and audit logging.
- Added CRM duplicate integration coverage for normalized email and phone detection, tenant isolation, successful merge, conflict choices, related task/opportunity reassignment, idempotency, read-only rejection, audit creation, and rollback on partial failure.
- Targeted local duplicate validation hung without output and was stopped after 30 seconds. A one-off local TypeScript check also hung without output and was stopped after 30 seconds. `GIT_PAGER=cat git diff --check` passed, so this checkpoint should be confirmed through GitHub Actions.
- GitHub Actions initially failed the duplicate checkpoint because the transaction helper selected PostgreSQL whenever `DATABASE_URL` existed, even for PGlite memory tests. Real PostgreSQL SQL clients are now explicitly marked before using `withTenantTransaction`.
- GitHub Actions passed for CRM duplicate merge fix commit `aa098ff`, including migrations, lint, typecheck, unit/integration tests, production build, and Playwright E2E. Next priority is extracting Opportunity Radar into a typed module.
- Started Opportunity Radar extraction into `src/modules/opportunity-radar/`: persisted typed alerts, deterministic rule sync, direct action links, dismissal with audit, automatic resolution, dashboard wiring, dedicated Radar page, and an integration test for rules, tenant isolation, dismissal, and resolution.
- Targeted Radar validation (`pnpm exec vitest run tests/opportunity-radar.test.ts`) and local `git diff --check` both hung without output during the Radar checkpoint and were stopped; validate this checkpoint through GitHub Actions after push.
- GitHub Actions passed for Opportunity Radar extraction commit `fe98f56`, including migrations, lint, typecheck, unit/integration tests, production build, and Playwright E2E. Next priority is the persisted workflow engine.
- Extracted the synchronous lead workflow into a persisted-definition execution path: tenant-scoped workflow lookup, normalized versioned definitions, typed workflow errors, repository helpers, an action registry, condition checks, idempotent event replay skipping, timeline step metadata, approval-required stops, waiting-state support, and registered initial actions for tasks, contact updates, tags, activities, mock email/SMS/WhatsApp, webhook mock, waits, and approvals.
- Seeded new tenants with the full `leadFollowUpWorkflow` definition instead of the old partial JSON shape, and added focused workflow-engine tests for persisted tenant behavior, replay idempotency, tenant isolation, and approval stops.
- Local workflow validation still hung (`pnpm exec vitest run tests/workflow-engine.test.ts`, one-off TypeScript, and `tsx` startup all stalled and were stopped), while targeted `git diff --check` passed.
- GitHub Actions passed for workflow engine checkpoint commit `46a0164`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added workflow run controls in `src/modules/workflows/`: tenant-authorized cancel, approve, reject, and manual retry services; server actions; Automatisations UI controls; control timeline entries; audit logs; and integration tests for status transitions and tenant isolation.
- Local Node validation was skipped for the workflow controls checkpoint because the local tooling remained unreliable; the changed files were manually inspected and staged explicitly.
- GitHub Actions passed for workflow controls commit `73cef7b`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added durable workflow resumption through `workflow.resume` domain events: waits schedule delayed resumes, approvals enqueue resumes after approval, manual retry replays the failed action, and cancelled runs skip queued resumes.
- Added workflow resume integration tests covering delayed resume, approval resume, cancellation blocking resume, and manual retry after a failed action.
- Local workflow-resume Vitest and one-off TypeScript checks hung and were stopped; tracked `git diff --check` returned clean before push.
- GitHub Actions initially failed commit `ba42a11` on a legacy controls fixture without an action cursor; commit `a27045a` keeps that historical control path compatible and both PR #1 CI runs passed end-to-end.
- Added worker polling runtime support: `WORKER_MODE=once|poll`, configurable batch size and interval, structured JSON startup/heartbeat/batch/shutdown logs, graceful `SIGTERM`/`SIGINT` shutdown, and tests for polling cadence, bounded sleeps, configuration parsing, and signal stop behavior.
- GitHub Actions passed for worker polling commit `64c9bf3`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added workflow dead-letter visibility: failed terminal `domain_events` now appear in Automatisations as tenant-scoped incidents with attempts, correlation IDs, timestamps, redacted errors, and test coverage for tenant isolation.
- GitHub Actions passed for workflow dead-letter commit `75d5740`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added persisted workflow step attempt metadata: migration/schema support for attempts, scheduled/start/completion timestamps, safe error summaries, repository insertion support, engine attempt numbering, and workflow-engine assertions.
- GitHub Actions passed for workflow step-attempt commit `a510be2`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added manual dead-letter recovery controls: failed terminal domain events can be requeued from Automatisations by authorized workflow operators, attempts are reset for a fresh worker retry window, safe errors are cleared, and an audit log records the action.
- Targeted local worker validation (`pnpm exec vitest run tests/workflow-worker.test.ts`) hung without output and was stopped after 30 seconds. `git diff --check` passed, so this checkpoint should be validated through GitHub Actions after push.
- Added domain event retry/backoff metadata: worker claims now record last attempted time, retries store computed delay and transient classification, terminal failures store max-attempt classification, manual requeue clears the metadata, and failed incidents surface safe failure labels.
- GitHub Actions initially caught a retry-metadata type mismatch and a SQL null-parameter issue in commits `ea706aa` and `b6d4950`; fix commit `04dfc73` passed migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added the first domain-specific worker handler beyond workflow resume: `connector.sync_requested` now dispatches the mock connector sync through the durable outbox path, updates connector health, writes sync/activity rows, and audits the action.
- GitHub Actions passed for connector sync domain-event handler commit `020051e`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Moved public lead follow-up onto the durable worker path: public lead ingestion now enqueues `lead.created`, the worker consumes it through the persisted workflow definition, creates the follow-up run/tasks/audit asynchronously, and tests explicitly run the worker where they expect workflow side effects.
- Targeted local lead-worker validation hung without output and was stopped after 30 seconds. Unstaged `git diff --check` also hung and was stopped, staged `git diff --cached --check` passed, and GitHub Actions passed for commit `3bf22e4`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added an Opportunity Radar worker handler: `opportunity_radar.sync_requested` now runs the persisted Radar detection path from the durable worker, refreshes alert rows, and records `opportunity_radar.synced` audit metadata.
- Targeted local Radar worker validation hung without output and was stopped after 30 seconds. Staged `git diff --cached --check` passed, and GitHub Actions passed for commit `865744b`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added workflow queue operations visibility: Automatisations now shows tenant-scoped queue status counts, pending/processing event details, retry/backoff metadata, safe error labels, and direct integration coverage for tenant isolation.
- Targeted local workflow-control validation hung without output and was stopped after 30 seconds. `git diff --check` and staged `git diff --cached --check` passed. GitHub Actions initially failed commit `1e24768` on JSX text lint, then passed for fix commit `b3a0f01`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added durable notification dispatch: workflow notification actions now create queued notification rows and `notification.dispatch_requested` events; the worker marks tenant-scoped notifications sent, audits dispatch, and rejects cross-tenant delivery attempts.
- Targeted local notification worker validation hung without output and was stopped after 30 seconds. Unstaged `git diff --check` and `git diff --stat` hung and were stopped; staged `git diff --cached --check` passed, and GitHub Actions passed for commit `7e4c4b6`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added workflow queue cancellation: authorized workflow operators can cancel active pending/processing domain events from Automatisations, moving them to `skipped`, removing them from active work, and recording audit metadata.
- Targeted local workflow-control validation hung without output and was stopped after 30 seconds. `git diff --check` and staged `git diff --cached --check` passed, and GitHub Actions passed for commit `5b6d282`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Hardened generic webhook delivery intake: idempotency keys are required, duplicate accepted deliveries are rejected, HMAC timestamps have an explicit replay window, oversized payloads are rejected, endpoint rate limiting is enforced, public route errors are safer, delivery payload logs redact sensitive fields, and accepted/rejected outcomes retain idempotency metadata.
- Targeted local connector validation hung without output and was stopped after 30 seconds. `git diff --check` and staged `git diff --cached --check` passed, and GitHub Actions passed for commit `56c241e`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added tenant-scoped webhook endpoint controls in Connexions: status/signature state display, HMAC secret rotation form, disable/re-enable actions, server-side authorization, audit logging, and tenant-isolation coverage.
- Targeted local connector module validation hung without output and was stopped after 30 seconds. `git diff --check` and staged `git diff --cached --check` passed, and GitHub Actions passed for commit `491f8ce`, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Secured webhook endpoint secrets by default: new tenants receive encrypted/hashed HMAC material immediately, legacy null-secret endpoints are secured lazily before configuration or delivery, unsigned deliveries are rejected, and Connexions now generates a strong one-time reveal secret instead of accepting manual cleartext entry.
- Targeted local connector validation hung without output and was stopped after 30 seconds. `git diff --check` and staged `git diff --cached --check` passed, and GitHub Actions passed for commit `29e5e55` on both push and PR runs, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added rejected-delivery recording for disabled webhook endpoints, safe public 403 mapping, and regression coverage that old HMAC secrets fail after generated secret rotation while the new secret succeeds.
- GitHub Actions caught a missing required phone field in the new rotation test on commit `7cac9f2`; fix commit `87ed175` passed both push and PR CI runs, including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added tenant-scoped webhook delivery history to Connexions with accepted/rejected status, safe reasons, idempotency metadata, redacted payloads, and direct module coverage in commit `3512c43`.
- Expanded webhook rejection security coverage for disabled endpoints, invalid signatures, rate-limit failures, payload redaction, and endpoint-scoped rate-limit isolation across tenants in commit `f56ab17`.
- Targeted local connector validation hung and was stopped after 30 seconds; unstaged `git diff --check` failed with an `mmap` cancellation while staged `git diff --cached --check` passed. Both GitHub Actions runs (`29194087123` push and `29194088593` pull_request) passed migrations, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added tenant-scoped workflow execution timelines to Automatisations with persisted action status, attempts, timestamps, redacted errors, repository/service reads, and integration coverage in commit `13635f2`.
- Targeted local workflow-control validation hung and was stopped after 35 seconds; staged `git diff --cached --check` passed. Both GitHub Actions runs (`29194406560` push and `29194407769` pull_request) passed migrations, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Expanded Playwright with a two-page draft/publication safety scenario: draft edits keep the immutable public snapshot and form online, visible unpublished state is asserted, publication swaps the live snapshot, an older publication can be restored without changing live until republished, and the form-created lead reaches CRM.
- Commit `81e7471` passed both CI runs (`29194632483` push and `29194633519` pull_request), including migration verification, lint, typecheck, unit/integration tests, production build, and the expanded Playwright E2E suite.
- Replaced the simulated `call_webhook` workflow action with idempotent durable delivery through `workflow.webhook_requested`: bounded HTTPS POST, worker retry/backoff, correlation/idempotency headers, safe errors, payload and target protections, success audit/activity records, and injected-fetch tests.
- Targeted local workflow-worker validation hung without output and was stopped; staged `git diff --cached --check` passed. Commit `32daccd` passed both CI runs (`29195174902` push and `29195175983` pull_request), including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added reusable atomic rate limiting under `src/modules/rate-limit/` with hashed subject/scope keys, PostgreSQL/PGlite and deterministic memory adapters, retry metadata, bounded cleanup, and operation/tenant isolation. Registration, login, password reset, invitation creation/acceptance, public forms, demo seeding, and inbound webhooks now enforce explicit policies.
- Targeted local rate-limit Vitest hung and was stopped after a reasonable wait; staged diff validation passed. Commit `0d84f39` passed both CI runs (`29196188525` push and `29196189381` pull_request), including migration verification, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
- Added bounded auth email delivery under `src/modules/email/`: French APP_URL-based templates, safe console/test providers, no raw tokens in logs or ordinary responses, identical password-reset responses, invitation delivery status, and authorized resend that replaces the previous token.
- Local targeted validation hung and was stopped. GitHub Actions caught and fixed optional preview-link typing in `9dc30da`; both final runs (`29196880873` push and `29196881800` pull_request) passed migrations, lint, typecheck, unit/integration tests, production build, and Playwright E2E.
