# Plateforme OAuth

Statut: non implémentée dans le premier checkpoint Phase 5.

Le prochain checkpoint doit fournir un fournisseur mock CI avec Authorization Code, PKCE, état unique, expiration, anti-rejeu, redirect URI exacte, scopes minimaux, chiffrement des jetons, verrou de rafraîchissement, révocation, reconnexion et audit sans contenu de jeton.

Aucun jeton OAuth n'est actuellement demandé, stocké ou renvoyé au navigateur.
